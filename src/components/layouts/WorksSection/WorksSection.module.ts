import { css } from "../../../../styled-system/css";

export const styles = {
    section: css({
        display: 'flex',
        padding: '3.906vw 3.125vw',
        justifyContent: 'space-between',
    }),
}